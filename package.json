{
  "name": "markterm",
  "version": "0.4.0",
  "description": "View Markdown + MermaidJS in the terminal: a full-screen viewer with clickable links that also opens HTML, images and PDFs, or inline images. For Ghostty, Kitty, WezTerm, iTerm2 and other terminals.",
  "keywords": [
    "markdown",
    "mermaid",
    "terminal",
    "viewer",
    "preview",
    "markdown-viewer",
    "html",
    "pdf",
    "kitty",
    "ghostty",
    "iterm2",
    "wezterm",
    "sixel",
    "herdr",
    "inline-image",
    "cli"
  ],
  "license": "MIT",
  "author": "Gospelo",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gospelo-dev/markterm.git"
  },
  "homepage": "https://github.com/gospelo-dev/markterm#readme",
  "bugs": {
    "url": "https://github.com/gospelo-dev/markterm/issues"
  },
  "type": "module",
  "bin": {
    "markterm": "dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "build": "rm -rf dist && tsc -p tsconfig.build.json && bun build src/cli.ts --outfile dist/cli.js --target node --external playwright --external shiki",
    "postbuild": "chmod +x dist/cli.js",
    "prepublishOnly": "bun run typecheck && bun test && bun run build"
  },
  "dependencies": {
    "marked": "^15.0.7",
    "playwright": "^1.63.0",
    "shiki": "^4.4.3"
  },
  "engines": {
    "node": ">=20",
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "bun-types": "^1.3.13",
    "typescript": "^5.8.2"
  }
}
